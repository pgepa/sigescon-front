# Servidor de testes / homologação com HTTP (front e API no mesmo esquema — sem HTTPS).
# URLs absolutas que o navegador consiga abrir (sem barra no final).
VITE_API_URL="http://10.96.30.15:8000/api/v1"
VITE_AUTH_API_URL="http://10.96.30.15:8000/api/v1"

# Ambiente futuro com HTTPS: use o mesmo esquema no site e na API (ou proxy no mesmo host),
# para evitar bloqueio por conteúdo misto.
# VITE_API_URL="https://api.seu-dominio.gov.br/api/v1"
# VITE_AUTH_API_URL="https://api.seu-dominio.gov.br/api/v1"
