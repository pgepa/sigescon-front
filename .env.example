# Produção: URLs absolutas que o *navegador* do usuário consiga acessar (sem barra no final).
# Use o mesmo esquema (HTTPS vs HTTP) do site do front para evitar bloqueio por conteúdo misto.

VITE_API_URL="https://api.seu-dominio.gov.br/api/v1"
VITE_AUTH_API_URL="https://api.seu-dominio.gov.br/api/v1"

# Rede interna (HTTP): só se o front também for servido em HTTP para os mesmos clientes.
# VITE_API_URL="http://10.0.0.1:8000/api/v1"
# VITE_AUTH_API_URL="http://10.0.0.1:8000/api/v1"
